# Supabase Configuration
# Import .env file into Netlify: Site settings > Environment variables > Import from .env

SUPABASE_URL=your_supabase_url_here
SUPABASE_ANON_KEY=your_supabase_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Note: Service role key should only be used server-side (Netlify Functions)
# Never expose the service role key to the client/browser
